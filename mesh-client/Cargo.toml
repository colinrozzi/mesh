[package]
name = "mesh-client"
version = "0.1.0"
edition = "2021"
description = "Client-side helpers for the mesh substrate — a plain Rust library an actor depends on to talk to its mesh node, instead of re-implementing the protocol."

[dependencies]
mesh-api = { path = "../mesh-api" }

[dev-dependencies]
