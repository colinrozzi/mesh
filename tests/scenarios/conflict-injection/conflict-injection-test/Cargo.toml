[package]
name = "mesh-conflict-injection-test"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "mesh-conflict-injection-test"
path = "src/main.rs"

[dependencies]
chat-protocol = { path = "../../../networks/chat/chat-protocol" }
mesh-testkit = { path = "../../../testkit" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
